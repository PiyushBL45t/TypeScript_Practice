/* 
To declare a piece of code that considers it reusability we can use interfaces in TypeScript.
Interfaces are parts of a code that are used as custom type declarations.

*/

// A car interface to declare a custom type of car

interface Car{
    fuelType : string,
    category : string,
    mileage : number,
    color : string,
    manufacturer : string
}



